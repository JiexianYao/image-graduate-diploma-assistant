version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      # ── 腾讯云 COS（必填） ──────────────────────────────────────────
      - COS_SECRET_ID=${COS_SECRET_ID}
      - COS_SECRET_KEY=${COS_SECRET_KEY}
      - COS_REGION=${COS_REGION:-ap-guangzhou}
      - COS_BUCKET=${COS_BUCKET}
      - PRESIGN_EXPIRE=${PRESIGN_EXPIRE:-3600}
      # ── LLM（可选） ────────────────────────────────────────────────
      - LLM_API_KEY=${LLM_API_KEY:-}
      - LLM_MODEL=${LLM_MODEL:-claude-3-5-sonnet-20241022}
      - LLM_API_URL=${LLM_API_URL:-https://api.anthropic.com/v1/messages}
      - LLM_API_STYLE=${LLM_API_STYLE:-anthropic}
      # ── 服务配置 ───────────────────────────────────────────────────
      - API_TOKEN=${API_TOKEN:-}
      - PORT=${PORT:-8080}
      # ── 分析配置 ───────────────────────────────────────────────────
      - MAX_CHARS_PER_FILE=${MAX_CHARS_PER_FILE:-3000}
      - MAX_TOTAL_CHARS=${MAX_TOTAL_CHARS:-15000}
    volumes:
      - ./ai/templates:/app/ai/templates
    # 不再需要本地 data/ 卷，所有持久数据存储在 COS
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/health')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
